"""Pure matching logic -- packages/core's matching engine.

No I/O, no side effects, no framework dependencies, per CLAUDE.md: every
function here takes fully-loaded domain objects in and returns a result out.
"""
